const User= require('./User'); // import the User model
const Thought = require('./Thought'); // import the Thought model
module.exports = { User, Thought }; // export an object with the User and Thought models as properties  
